# CPU-only PyTorch — Render free tier has NO GPU
# Install torch first from CPU index to avoid downloading 1.3GB of CUDA libs
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

chromadb>=1.0.0
sentence-transformers>=3.0.0
groq>=0.4.0
reportlab>=4.0.0
python-dotenv>=1.0.0
fastapi>=0.100.0
uvicorn>=0.20.0
edge-tts>=6.1.9
